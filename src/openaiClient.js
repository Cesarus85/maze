export async function fetchMazeFromBackend(params) {
  // Später: fetch('/api/maze', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(params) })
  // Hier: Stub -> null, damit main.js lokal generiert
  return null;
}
